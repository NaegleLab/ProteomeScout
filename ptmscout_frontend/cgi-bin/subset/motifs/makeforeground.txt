#!/usr/bin/python
import cgi
#import cgitb;cgitb.enable()
print "Content-Type: application/octet-stream\n"
import sys

from pathname import *
form = cgi.FieldStorage()
num = form.getvalue('num',None)
filename = motifPath+"foreground%s" % (str(num))
# Actual File Content will go hear.
try:
	fo = open(filename, "rb")
	str = fo.read();
	# Close opend file
	fo.close()
except IOError:
       "Foregrount file not found"
       str = ''

print str

